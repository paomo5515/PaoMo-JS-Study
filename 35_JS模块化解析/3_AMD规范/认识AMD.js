/*
    AMD规范
    AMD主要是应用于浏览器的一种模块化规范：
      AMD是 Asynchronous Module Defintion（异步模块定义）的缩写；
      它采用的是 异步加载模块；
      事实上 AMD 的规范还要遭遇 CommonJS，但是 CommonJS目前依然在被使用，而AMD使用的较少了；
    我们提到过，规范只是定义代码的应该如何去编写，只有有了具体的实现才能被应用；
      AMD实现的比较常用的库就是 require.js和 curl.js
*/